public enum Strings {
    // Common
    enum Common: String {
        case space = " "
        case enter = "\n"
        case quotes = "\""
        case divider = "|"
    }
    // Localized
    enum Localized: String {
        case numberOfFeedbacks = "numberOfFeedbacks"
        case itemType = "sparViewController.itemLable.itemType"
        case itemName = "sparViewController.itemLable.itemName"
        case itemDescription = "sparViewController.itemLable.itemDescription"
        case itemCountry = "sparViewController.itemLable.country"
        case description = "sparViewController.itemLable.description"
        case info = "sparViewController.itemLable.info"
        case features = "sparViewController.itemLable.features"
    }
    // Countries ISO
    enum CountriesISO: String {
        case spain = "ESP"
    }
}
